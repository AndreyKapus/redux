export const NavBar = () => {
    return(
        <div>
            <h1>test redux app</h1>
            <div>log in</div>
            <button type="button">Log in</button>
        </div>
    )
}

export default NavBar;