export const NavBar = () => {
    return(
        <div>
            <h1>test redux app</h1>
            <div>log in</div>
            <form>
                <input type="text" name="login"/>
                <button type="submit">Log in</button>
            </form>
            
        </div>
    )
}

export default NavBar;